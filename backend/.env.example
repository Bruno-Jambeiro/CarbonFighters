# ==================================
# DATABASE CONFIGURATION
# ==================================
# With Docker: use these values exactly as they are
# Without Docker: change according to your local PostgreSQL installation

# Development database
DB_HOST=localhost
DB_PORT=5432
DB_USER=carbonfighters_user
DB_PASSWORD=carbonfighters_pass
DB_DATABASE=carbonfighters

# Testing database
DB_PORT_TEST=5433
DB_DATABASE_TEST=carbonfighters_test

# ==================================
# JWT (Authentication Tokens)
# ==================================
# ⚠️ IMPORTANT: Change this secret in production!
# Generate a secure secret with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
TOKEN_SECRET=your_super_secret_for_jwt_change_it_in_production

# ==================================
# SERVER CONFIGURATION
# ==================================
PORT=3000
NODE_ENV=development

# ==================================
# 🔒 SECURITY REMINDER
# ==================================
# This file (.env.example) is a TEMPLATE and goes in Git
# Your actual .env file should NEVER be committed to Git
# 
# To create your .env file:
#   cp .env.example .env
#   cp .env.example .env.test
#
# For production/staging, use DIFFERENT and STRONGER credentials
# Read SECURITY.md for more details
